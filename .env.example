# General settings
HOST=0.0.0.0
PORT=3001
IN_DOCKER=0
LOG_LEVEL=info
BASIC_HTPASSWD=/.htpasswd

# Browser settings
BROWSER_TYPE=chromium
BROWSER_CONTEXT_LIMIT=20
SCREENSHOT_TYPE=jpeg
SCREENSHOT_QUALITY=80

# Path settings (optional - defaults to project directories)
# USER_DATA_DIR=/path/to/user_data
# USER_SCRIPTS_DIR=/path/to/user_scripts
# APP_DIR=/path/to/app

# SSL settings (optional - leave empty if not using SSL)
# SSL_KEYFILE=/.ssl/key.pem
# SSL_KEYFILE_PASSWORD=
# SSL_CERTFILE=/.ssl/cert.pem
# SSL_CIPHERS=TLSv1

# Uvicorn settings
UVICORN_WORKERS=2
DEBUG=0

# Version info (set automatically by Docker build)
# GIT_TAG=v0.0.0
# GIT_SHA=
